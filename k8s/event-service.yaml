apiVersion: v1
kind: Service

metadata:
  name: event-service
  namespace: coursework

spec:
  selector:
    app: event-service

  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001

  type: ClusterIP